# E4: two-qubit, simple interaction, multiple channels

data:
  output_path: data/E4_dataset.npz
  dataset_path: data/E4_dataset.npz

physics:
  system_type: two_qubit
  observables: [sigma_z1, sigma_z2]
  gamma_range: [0.1, 1.0]
  J_range: [0.1, 1.0]

model:
  type: transformer

training:
  n_epochs: 50

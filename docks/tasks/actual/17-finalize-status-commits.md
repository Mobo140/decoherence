# Task: Финализация — актуализировать статус и разложить коммиты

## Why

Внутренние доки over-claim'ят («Paper 1 сдан», «проект готов»), тогда как статьи —
черновики. После задач 13–16 нужно привести статус к реальности и зафиксировать
работу чистыми коммитами (сейчас вся история — один коммит «MVP»).

## Scope

**Входит:**
- `docks/current/CURRENT_STATUS.md` — отразить реальное состояние: код прибран,
  Paper 1 доведён (рисунки/библио/авторы/факт-чек), Paper 2 + теория QPT. Без «сдан».
- `docks/tasks/actual/EXECUTION_PLAN.md` — перенести 13–16 в закрытый бэклог с итогами.
- Перенести закрытые 13–16 в `docks/tasks/archive/`, обновить archive `README.md`.
- Коммиты (раздельно, с атрибуцией): (1) уборка кода/venv/qutip; (2) Paper 1;
  (3) Paper 2 теория; (4) docs/status.

**Не входит:**
- Push (только по явной просьбе); новые фичи.

## Acceptance Criteria

- [ ] `CURRENT_STATUS.md` описывает фактическое состояние, без «Paper 1 сдан»
- [ ] Задачи 13–16 в `archive/`, `EXECUTION_PLAN.md` показывает их как done с итогом
- [ ] `git status` чист (нет `.venv`, нет случайных артефактов)
- [ ] Коммиты логически раздельные, сообщения с `Co-Authored-By` футером
- [ ] `pytest tests/` зелёный; `make pdf` собирает обе статьи

## Invariants

- IV1: Не пушим без явной команды пользователя
- IV2: Чемпионские CSV и метрики не меняются

## Verification

```bash
git log --oneline -6
git status -s
pytest tests/ -q && make pdf
```

## Status

`planned`

## Priority

P2

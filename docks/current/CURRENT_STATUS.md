# Current Status — Quantum Decoherence Predictor

## Проект прибран, статьи доведены (2026-09-13)

Очередь `docks/tasks/actual/` пуста — бэклог 01–17 в архиве.
`pytest tests/` зелёный (92), 0 warnings.

История git до 2026-09-12 состояла из одного коммита `MVP` (2026-01-03) — он
фиксировал только ранний прототип; ~9 месяцев реальной работы (DDD-структура,
обе статьи, `docks/`, весь `experiments/`) не коммитились. Задача 13 это закрыла
снимком текущего состояния; дальше — обычная история отдельными коммитами.

### Статьи — реальное состояние

**Paper 1** (`paper/paper_1/`, 1 кубит, сценарии A–C) — доведена до состояния,
пригодного к сабмиту: 6 publication-рисунков (было 0), библиография 12 ссылок
(каждая проверена веб-поиском перед добавлением — не выдумано ни одной), блок
авторов, явные claims C1–C5 в тексте, факт-чек (abstract утверждал «>15×»
против inverse-baseline, по таблице E5 реально 3–9×). `make pdf-1` — чисто.
**Не проверено человеком** — компилируется и внутренне непротиворечиво, но нет
рецензии/вычитки автором перед реальной подачей.

**Paper 2** (`paper/paper_2/`, 2 кубита, XXZ/TFIM, D/E) — лучший валидный TFIM
cross-system **E8c R²=0.575** (цель 0.70 не взята). Раньше это было заявлено
как «потолок смеси J у QPT» одним предложением без обоснования; теперь есть
теория: точная диагонализация N=2 TFIM-гамильтониана (блок-диагонализация по
чётности $P=\sigma_x\otimes\sigma_x$) + scaling-аргумент через отношение
расщеплений секторов $r(J)$, честно отмечено, что N=2 не даёт истинной
неаналитичности (конечно-размерный предвестник, не сам QPT). Тот же набор
правок, что в Paper 1 (авторы, битая библиография, overfull hbox), починен
заодно. `make pdf-2` — чисто.
**XXZ R²=0.817** (E11a) без изменений.

`description_ru.tex` — черновик на русском, таблица D/E там **устарела**
(до E8/E11), не обновлялась в этом цикле.

### Как запустить

```bash
source .venv/bin/activate
python app.py          # http://localhost:7860  — макет аудита, 7 экранов
pytest tests/
make pdf                # обе статьи → paper/paper_1/main.pdf, paper/paper_2/paper2.pdf
```

Replay эксперимента: вкладка **02 Runs** → E8 → fast. Не запускать E6/E8 full
из UI — перезапишут чемпионские CSV.

### Чемпионы (без изменений в этом цикле)

- A physics_only E1 **0.9995** (было записано 0.9998 — расходилось с
  `e1_ablation_p1.csv` (0.99949767) и со статьёй; исправлено в задаче 20)
- B physics_lstm E1 0.945
- C physics_lstm E1 0.981
- D transformer E11a 0.817 / E8c 0.740
- E physics_lstm E8c **0.575** (Transformer E7a 0.599)

### Открытые пункты (не блокеры, на будущее)

- Реальная аффилиация автора вместо placeholder «Independent Researcher» —
  в обеих статьях.
- `description_ru.tex` не синхронизирован с финальными числами D/E.
- `lstm_predictor.py` (780 LOC) — крупнейший файл, кандидат на разбиение,
  не трогался.
- Пренебрежимо малый overfull hbox (5.4pt, Table 2/Comparison) в Paper 1 —
  не фиксировался, не влияет на читаемость.

### Что сознательно не делаем

- YAML-пайплайн вместо `experiments/eN_*.py`
- Multi-seed ×5 / eval-v1 перепрогон (часы CPU)
- Новые E12–E16 из аудита (entanglement-фичи, 1/f, плотный J-sweep, ECE)
- Lineage чемпионов / pydantic-схема CSV
- revtex-миграция статей
</content>
